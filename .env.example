# 复制成 .env 后填入真实值。.env 已在 .gitignore 里，不会被提交。
#
#   cp .env.example .env
#   # 填好之后
#   set -a && source .env && set +a
#
# 也可以直接 export：
#   export CODESENSE_API_KEY=sk-xxxx

# SemCon 抽取、意图判定等要用的 LLM key。
# 默认对接 dashscope（见 configs/default.yaml 里的 llm.base_url）。
CODESENSE_API_KEY=sk-replace-me
