{
  "queries": {
    "feature_localization": [
      "Find the entry function that handles user login authentication.",
      "Locate the core method that creates an order and writes it to the database.",
      "Search for modules or functions responsible for cache invalidation strategy.",
      "Find all implementations related to retry mechanisms in the project.",
      "Locate the code path that parses configuration files and builds runtime objects."
    ],
    "call_chain_tracing": [
      "Find which disk-related functions are called by init_readahead.",
      "Trace downstream from handle_request to the function that actually executes SQL.",
      "Find all upstream callers of malloc_disk.",
      "Search for the complete call chain from API controller to repository.",
      "Find all entry points that trigger security_check."
    ],
    "constraint_filtering": [
      "Search for functions related to readahead within the disk module.",
      "Find security-related functions while excluding test files.",
      "Search for methods whose return type is bool and whose names contain validate.",
      "Find cache-related classes while excluding deprecated implementations.",
      "Search for functions handling timeout in the network module."
    ],
    "semantic_synonym_queries": [
      "Find code implementing user registration (possibly named create account or sign up).",
      "Search for rate-limiting logic (possibly written as rate limit or throttle).",
      "Find circuit breaker implementations (possibly named circuit breaker or fail fast).",
      "Find cache warm-up related code (possibly named warmup or prime cache).",
      "Search for graceful shutdown logic (possibly named graceful shutdown or drain)."
    ],
    "refactor_and_bugfix_support": [
      "Find all functions that directly manipulate global state for decoupling refactor.",
      "Search for access paths that may have null pointer risks.",
      "Find key functions where uncaught exceptions are directly propagated.",
      "Locate duplicate implementations of token parsing logic.",
      "Search for hotspot functions with high complexity and multiple nested if-else blocks."
    ]
  }
}
